#include "main.h"
/**
 * mul - checks for checks for a digit (0 through 9).
 * @e: e - Variable
 * @f: f - variable
 * Return: Always 0.
 */
int mul(int e, int f)
{
	int mul;

	mul = e * f;

	return (mul);
}
